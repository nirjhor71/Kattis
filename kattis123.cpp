#include<bits/stdc++.h>
#include<math.h>
using namespace std;
int main()
{
    string s;
    int t,sum=0,k;
    cin>>t;
    while(t--)
    {
        cin>>s;
        int a=atoi(s.c_str());
        int len=s.size();
        for(int i=0;i<len;i++)
        {

        }
    }
    cout<<sum<<endl;
}
